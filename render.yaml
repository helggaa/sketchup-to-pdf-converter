services:
  - type: web
    name: sketchup-to-pdf-backend
    runtime: docker

    plan: free

    dockerfilePath: ./Dockerfile

    envVars:
      - key: FRONTEND_URL
        value: https://YOUR-FRONTEND.vercel.app